(* while.cm Build project *)
Library
    structure While
    structure WhileParser
    structure WhileLrVals
    structure WhileParser
    structure AST
is
    while_ast.sml (* Datatypes for ast *)
    while.lex (* Lexer rules. *)
    while.grm (* Parser rules. *)
    integrate.sml (* Build the parser *)
    compiler.sml (* parse my input and execute instructions *)
    $/basis.cm 
    $/smlnj-lib.cm
    $/ml-yacc-lib.cm 
    $smlnj/compiler/compiler.cm 
